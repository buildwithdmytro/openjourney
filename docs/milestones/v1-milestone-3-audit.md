# V1 milestone 3 completion audit

Authoritative scope: Phase 3 in [`v1-milestone-3-plan.md`](./v1-milestone-3-plan.md).

| Requirement | Evidence required | Current status |
|---|---|---|
| **Journey Schema & Draft CRUD** | Migrations, domain models, scopes allowlist, database store, HTTP endpoints, React nav/scaffold, OpenAPI yaml | Complete; `015_journeys.sql` migration, domain structs in `internal/domain/domain.go`, RBAC permissions in `allowedPermissions`, HTTP routes wired in `internal/httpapi/journeys.go` and `server.go`, React scaffold in `web/src/sections/Journeys.tsx`, and OpenAPI spec linting via Redocly. |
| **Graph Model & AST Validation** | Parser, AST config, Validator, Golden fixtures, Publish service, Blob freeze | Complete; `internal/journey/graph.go` & `nodes.go` define the graph AST, `validate.go` validates node count, unreachable nodes, branch contracts, and parse durations. Immutable publish creates versions in `journey_versions` and uploads graph JSONs to MinIO (`internal/journey/publish.go`). |
| **Durable Runtime Core** | State schemas, store transactions, Clock seam, stateless worker loop | Complete; `016_journey_runtime.sql` migration defines runs, steps, and transitions. `AdvanceRunTx` in `internal/postgres/journey_runtime.go` atomically updates run state, completes the claimed step, inserts the next step, and records transition history. Stateless workers claim separate due steps safely using `SKIP LOCKED`; each `TickNext` call executes one claimed node using a `Clock` interface (`RealClock`/`FakeClock`). |
| **Node Executors & Visual Builder** | Core node executors, evaluator Matches, React Flow diagram builder | Complete; `internal/journey/nodes.go` executes `entry`, `delay`, `condition`, `split`, `action`, `goal`, `exit`, `message`, and `wait_event` nodes. `Matches` evaluates profile/consent/ClickHouse against a single subject ID without full table scans (`internal/audience/evaluate.go`). React Flow editor (`web/src/sections/Journeys.tsx`) provides drag-and-drop DAG building, sidebar configuration, client-side validation, and publishing. |
| **Triggers, Re-entry & Waits** | Event-triggered enrollment, wait-for-event, timeout steps, segment schedule enrollment | Complete; ProjectEvent in `internal/postgres/store.go` matches events to active versions, checks re-entry rules, and creates runs. `wait_event` executor schedules a timeout step and parks the run in waiting. Event ingestion inserts a success step and cancels the timeout step. `EnrollScheduledDue` resolves segment members and schedules them periodically. |
| **Message Delivery & Policies** | Intents schema, message node executor, delivery loop, timezone DST quiet hours, fatigue cross-channel UNION | Complete; `017_journey_delivery.sql` defines message intents. `DeliverNext` claims intents, evaluates policy, applies timezone-aware quiet hours, renders Liquid templates, rewrites tracked links, and sends via SES/Webhook. Cross-channel fatigue check (`postgres/delivery.go` `SentCountSince`) UNIONS campaign `delivery_attempts` and journey `journey_message_intents`. |
| **Operator Tools** | Version pause/resume, participant cancel, DLQ, retry retryable failures, segment backfill, late catch-up policies | Complete; HTTP endpoints allow pausing/resuming versions, canceling runs, fetching DLQ, retrying dead steps/intents, and backfilling. `TickNext` checks stale steps and applies `run`, `skip`, or `reschedule` catch-up policies. Web operator UI provides pause/resume toggles, timeline inspector, and DLQ retry actions. |
| **Fake-Clock, Replay, Determinism & Load tests** | Fake-clock simulation, replay validation, event race tests, worker-kill load smoke, Prometheus metrics | Complete; `TestJourneysFakeClockEndToEnd` runs end-to-end fake-clock simulation. `TestJourneysReplayCompatibility` asserts byte-identical transition histories. `TestJourneysDeterminism` verifies event races. `./scripts/smoke-journeys.sh` runs 500-participant load under mid-flight worker kills. Exposes six OTel counters (`openjourney_journey_*`) on `:8084/metrics` of the worker. |
| **Critical Carry-Over Hardening (§8)** | `retryable_failed` CHECK lists, single-process adapters, deterministic splits, fail-fast | Complete; added `retryable_failed` to CHECK constraints, instantiated SES adapters once per process in `cmd/journeys-worker`, implemented SHA256-based deterministic split bucketing, validated config parse durations, verified `@xyflow/react` audit cleanliness, and registered OpenAPI schemas. |

The milestone is complete only when every row is supported by direct current-state evidence.
