# V1 milestone 4 completion audit

Authoritative scope: Phase 4 experimentation and analytics in [`v1-milestone-4-plan.md`](./v1-milestone-4-plan.md).

| Requirement | Evidence required | Current status |
|---|---|---|
| **9.0.1 Workspace/app isolation** | Event enrollment, wait resolution, transition reads, and ClickHouse matching are workspace/app scoped | Complete; `TestJourneyWorkspaceIsolation` in `internal/postgres/journey_runtime_integration_test.go` proves two-workspace isolation for enrollment, wait resolution, and transition reads; `internal/audience/evaluate.go` scopes ClickHouse matching by workspace and app. |
| **9.0.2 Poison-pill DLQ** | Repeated lease expiry reaches `dead` and appears in the journey DLQ | Complete; `ClaimJourneyStep` in `internal/postgres/journey_runtime.go` caps timed-out reclaims, and `TestJourneyDLQPoisonPill` proves the poisoned step becomes dead and is returned by the DLQ query. |
| **9.0.3 `provider_sent` reconciliation** | Reconciliation bypasses policy and repeated emission failure dead-letters | Complete; `internal/journey/deliver.go` emits directly from `provider_sent`; `TestDeliverNext_ProviderSentEmissionFailureDeadLetters` and its suppression subtest prove the send event is not dropped and repeated failure terminates. |
| **9.0.4 `always` re-entry deduplication** | One enrollment per deterministic schedule firing | Complete; `internal/journey/enroll.go` derives the sequence from the firing slot, and `TestEnrollScheduledAlwaysOncePerFiring` proves busy-loop retries deduplicate while a later firing enrolls again. |
| **9.0.5 Batched correctness fixes** | Deterministic app selection, external subjects, quiet-hour validation, replay-safe actions, consistent lock order | Complete; ordered app lookup and deterministic action-event keys are in `internal/postgres/journey_runtime.go`; `TestBackfillSetsSubjectExternalID`, `TestEnrollScheduledDue`, `TestIsInQuietHoursRejectsEqualBounds`, and `TestExecuteActionProfileUpdate` cover the observable properties; transition and wait paths lock run before step. |
| **9.0.6 Journey `message.sent` contract** | Journey sends validate without a campaign ID but missing-source events fail | Complete; `domain.Event.Validate` accepts `journey_id` as the source and `TestEventValidate` in `internal/domain/domain_test.go` covers both positive and negative contracts. |
| **9.0.7 Human-actor negative gate** | API-key publish and backfill requests receive 403 | Complete; `TestJourneyPublishAndBackfillRequireHumanActor` proves neither operation proceeds for an API-key actor; the corrected Milestone 3 audit no longer overstates transactional scope or worker concurrency. |
| **9.1.1 Experiment migration and scopes** | Experiment tables plus all scopes in migration defaults and RBAC | Complete; `018_experiments.sql`, `internal/auth/rbac.go`, and authenticated routes contain `experiments:read`, `experiments:write`, and `reports:read`; `TestExperimentMigrationAndDefaultScopesIntegration` proves a fresh key receives them. |
| **9.1.2 Experiment domain models** | Snake-case experiment, variant, and assignment models | Complete; `Experiment`, `ExperimentVariant`, and `ExperimentAssignment` are defined in `internal/domain/domain.go` and compile through the store/API layers. |
| **9.1.3 Experiment store CRUD** | Scoped CRUD, conflict-safe assignment, immutable running seed | Complete; `internal/postgres/experiments.go` implements tenant/workspace-filtered CRUD and `ON CONFLICT DO NOTHING` assignment; `TestExperimentStoreIsolationAssignmentAndSeedGuard` proves isolation, stability, and seed immutability. |
| **9.1.4 Deterministic assignment library** | SHA-256 stability and weighted distribution tests | Complete; `internal/experiment/assign.go` contains the shared SHA-256 bucket implementation; `TestAssignStableAcrossRepeatedCallsAndRestartEquivalent` and `TestAssignHoldoutAndWeightedDistribution` cover stability and allocation. |
| **9.1.5 Experiment HTTP and React scaffold** | Scoped CRUD routes and working create/list UI | Complete; routes are in `internal/httpapi/server.go`, handlers in `experiments.go`, and UI/API clients in `web/src/sections/Experiments.tsx` and `web/src/api.ts`; focused Go and React tests exercise create/list. |
| **9.1.6 Experiment OpenAPI** | All CRUD routes and schemas lint cleanly | Complete; `openapi/openapi.yaml` documents experiment CRUD and schemas; the closeout build retains the earlier clean Redocly lint result recorded in the implementation plan. |
| **9.2.1 Experiment bindings migration** | Binding/stamp columns and complete disposition constraints | Complete; `019_experiment_bindings.sql` adds campaign, delivery-attempt, and journey-intent fields; `TestExperimentBindingsMigrationIntegration` inspects every column and constraint. |
| **9.2.2 Campaign variant resolution** | Assignment, template choice, stamps, and unsent holdout | Complete; `internal/campaigns/deliver.go` resolves experiment variants; `TestDeliverNext_ExperimentVariantsAndHoldout` proves the weighted split, stamps, template selection, and zero provider sends for holdout recipients. |
| **9.2.3 Journey variant resolution** | Split/message assignment reuse, template/stamps, holdout handling | Complete; `TestExecuteExperimentSplitMatchesSharedAssignment` and `TestExecuteExperimentMessageSelectsTemplateStampsAndHoldsOut` cover persisted assignments, selected templates, intent/event stamps, and terminal holdouts. |
| **9.2.4 Assignment telemetry** | Variant-labeled counter increments only for a new authoritative assignment | Complete; `openjourney_experiment_assignments_total` is defined in `internal/telemetry/telemetry.go`; `TestExperimentAssignmentCounterIncludesVariant` verifies its value and label while replays remain uncounted. |
| **9.3.1 Analytics facts migration** | Fact tables, frozen-goal columns, constraints, unique keys, and indexes | Complete; `020_analytics_facts.sql` supplies the schema, and `TestAnalyticsFactsMigrationIntegration` verifies tables, enums, uniqueness, and report indexes. |
| **9.3.2 Goal freeze** | Dispatch/publish snapshots survive later live-config edits | Complete; campaign dispatch and journey publish persist goal/window snapshots; `TestConversionGoalsFreezeAtCampaignDispatchAndJourneyPublish` proves subsequent experiment and draft edits do not change them. |
| **9.3.3 Attribution projection** | Frozen-goal bounded lookup, window enforcement, value extraction, and idempotency | Complete; `projectConversionFact` in `internal/postgres/fact_projection.go` runs inside `ProjectEvent`; `TestProjectEventAttributesFrozenGoalWithinWindowIdempotentlyAndByWorkspace` proves scoped last-touch attribution, in/out-of-window behavior, value extraction, and replay deduplication. |
| **9.4.1 Engagement projection** | Five engagement types projected with correct source metadata and idempotency | Complete; `projectEngagementFact` projects delivered/opened/clicked/bounced/complained events; `TestProjectEventProjectsEngagementFactsIdempotentlyAndByWorkspace` proves campaign/journey metadata, workspace isolation, and one row on replay. |
| **9.4.2 Funnel and deliverability reports** | Total/unique funnels, rates, scope isolation, and indexed Postgres reads | Complete; `internal/postgres/analytics.go` queries disposition/fact tables through `s.pool`; `TestCampaignAndJourneyReportsExactCountsAndWorkspaceIsolation` proves exact counts, rates, scoped misses, and covering indexes. |
| **9.4.3 ClickHouse-free report endpoints** | Typed campaign/journey JSON, reports scope, 404 on scoped miss | Complete; `GET /v1/reports/campaigns/{id}` and `/journeys/{id}` use the Postgres store without `SetClickHouse`; `TestReportEndpointsReturnJSONAndPassScopedPrincipal`, scope, and not-found tests cover the contract. |
| **9.5.1 Frequentist statistics** | Documented two-proportion z-test matches known values | Complete; `internal/experiment/stats.go` implements the frequentist calculation; textbook and edge-case coverage is in `internal/experiment/stats_test.go`. |
| **9.5.2 Experiment report** | Per-variant conversion/uplift/significance and guardrail data | Complete; `Store.ExperimentReport` and `GET /v1/reports/experiments/{id}` provide the report; `TestExperimentReport` validates seeded per-variant results. |
| **9.5.3 Advisory winner and approved rollout** | Significance/guardrail recommendation plus human-only immutable rollout | Complete; `TestRecommendWinnerRequiresSignificanceAndHealthyGuardrails` keeps the winner advisory; `TestExperimentRolloutRequiresHumanActorAndReturnsNewVersion` rejects API keys, while `TestExperimentReport` proves an approved rollout creates a distinct campaign pinned to the winning template. |
| **9.6.1 Experiments authoring UI** | CRUD, variants, holdout, and campaign/journey binding round-trip | Complete; `web/src/sections/Experiments.tsx` implements authoring and `Experiments.test.tsx` proves variant, holdout, and campaign binding round-trip behavior. |
| **9.6.2 Reports UI** | Inline SVG funnel, deliverability, uplift/significance, report links, both themes | Complete; `web/src/sections/Reports.tsx` uses dependency-free inline SVG and exposes advisory significance states; `Reports.test.tsx` proves exact API values and light/dark rendering, while `App.test.tsx` covers report navigation. |
| **9.7.1 Determinism closeout** | Same assignment across processes, distribution tolerance, and unsent holdout | Complete; `TestAssignStableAcrossFreshProcesses`, `TestAssignHoldoutAndWeightedDistribution`, and `TestDeliverNext_ExperimentVariantsAndHoldout` directly prove all three properties. |
| **9.7.2 Attribution closeout** | In/out window, summed revenue, duplicate-event idempotency | Complete; `TestProjectEventAttributesFrozenGoalWithinWindowIdempotentlyAndByWorkspace` records two in-window purchases totaling 60, rejects the out-of-window value, and keeps one fact for a replay. |
| **9.7.3 Report-accuracy integration** | Projection-driven exact funnel, uplift, deliverability, and winner values | Complete; `TestReportAccuracyFromProjectedEvents` projects 75 real Postgres events and asserts exact funnel totals/uniques, bounce/complaint rates, conversion rates, uplift, p-value, and recommendation. |
| **9.7.4 Load/performance** | Performance evidence or an explicit documented waiver | Waived by explicit user direction on 2026-07-14; performance tests were not run, and this milestone makes no latency, throughput, or sub-linear scaling claim. |
| **9.7.5 Telemetry closeout** | Assignment and attributed-conversion counters with replay-safe semantics | Complete; both counters are defined in `internal/telemetry/telemetry.go`; assignment and live-Postgres attribution metric tests prove labels, committed-insert counting, and no increment on conflict/out-of-window projection. |
| **9.7.6 Full closeout suite** | Go tidy/build/vet/test, frontend typecheck/test/build, dependency audit | Complete on 2026-07-14; `go mod tidy`, `go build -buildvcs=false ./...`, `go vet ./...`, and `go test ./...` passed; frontend typecheck, 26 tests, and production build passed; `npm audit --audit-level=high` found 0 vulnerabilities. |
| **9.7.7 Completion audit** | M2/M3-format requirement table with direct current-state evidence | Complete; this document traces every Milestone 4 task, including all seven 9.0 hardening fixes and the explicit 9.7.4 performance waiver. |

Milestone 4's experimentation and analytics slice is complete with the explicit performance-test waiver above. PostgreSQL remains the only reports dependency; fact projection is idempotent and scan-free; assignments are deterministic; attribution uses frozen goals/windows; and rollout remains separately human-approved.
