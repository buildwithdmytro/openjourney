# V1 milestone 2 completion audit

Authoritative scope: Phase 2 in [`v1-milestone-2-plan.md`](./v1-milestone-2-plan.md).

| Requirement | Evidence required | Current status |
|---|---|---|
| **Segment & Audience Compiler** | Schema definitions, compiler tests, SQL generation, exclusion handling | Complete; `009_audiences.sql` migration, three-leg audience compiler in `internal/audience/compile_pg.go` and `compile_ch.go`, and comprehensive query compilation golden tests |
| **Template & Liquid Rendering** | Schema definitions, Liquid rendering, tracking links, sandbox validation | Complete; `010_templates.sql` migration, Liquid template rendering with link decoration/signature in `internal/render/render.go` and `links.go`, and comprehensive rendering tests |
| **Policy & Suppression Engine** | Suppression, consent, and fatigue checks; postgres store checks; policy decision logging | Complete; `011_delivery_policy.sql` migration, delivery policy engine in `internal/policy/policy.go` synchronously verifying consent ledger, suppression entries, and hard-coded fatigue caps |
| **Channel Adapters** | Interface definition, Fake/Mock adapter, rate-limited SES, signed HMAC webhooks with SSRF guard | Complete; `ports.ChannelAdapter` interface, fake adapter in `internal/channels/fake.go`, AWS SES rate-limited adapter with token bucket in `internal/channels/ses.go`, and HMAC-signed webhook adapter with DNS re-resolution SSRF guard in `internal/channels/webhook.go` |
| **SES/SNS Callback Handler** | SNS signature verification, SubscriptionConfirmation, event ingestion | Complete; SES webhook endpoint `POST /v1/callbacks/ses` in `internal/httpapi/callbacks.go` verifies SNS X.509 certificates (restricted to region-specific sns hosts), processes subscriptions, and ingests bounces/complaints to suppression list |
| **Campaign Dispatcher** | Schema definition, MinIO immutable manifest creation, job sharding | Complete; `012_campaigns.sql` migration, dispatcher daemon in `cmd/campaigns-dispatcher` and `internal/campaigns/dispatch.go` that compiles segment manifests, puts them to MinIO, and shards delivery jobs |
| **Delivery Worker** | Job lease claims, effectively-once delivery attempt guard, policy short-circuits | Complete; delivery daemon in `cmd/campaigns-delivery` and `internal/campaigns/deliver.go` which locks delivery jobs, checks attempts on-conflict to ensure effectively-once delivery, and evaluates policies |
| **True End-to-End Integration** | Driving real dispatcher and delivery paths with fake adapter | Complete; `TestCampaignsEndToEnd` in `internal/postgres/campaigns_integration_test.go` executes campaign creation, dispatching, manifest upload, sharded delivery, and event emission |
| **Campaign Unit Tests** | `internal/campaigns` logic coverage for all code paths | Complete; unit tests in `internal/campaigns/deliver_test.go` assert effectively-once skip, policy-rejection short-circuit, render-failure paths, and transient-send-failure requeues |
| **Worker Failure & Recovery Smoke** | Multi-shard load, mid-shard worker termination, no double-send validation | Complete; `./scripts/smoke-campaign-delivery.sh` runs a sharded campaign broadcast, terminates the delivery worker mid-execution, restarts it, and asserts zero duplicate sends |
| **Reproducibility Test** | Restoring segment from MinIO manifest produces identical checksum | Complete; `RedispatchFromManifest` in `internal/campaigns/dispatch.go` and integration tests in `campaigns_integration_test.go` verify manifest-based dispatch reproduces identical sets |
| **Explainability Test** | Exactly one delivery attempt per recipient with clear reason | Complete; `TestCampaignsExplainability` in `campaigns_integration_test.go` asserts that every evaluated recipient has precisely one delivery attempt row with a non-empty decision/reason |
| **OpenTelemetry Delivery Telemetry** | OTel counters, metrics server on /metrics | Complete; global counters (`openjourney_delivery_messages_sent_total`, `openjourney_delivery_bounces_total`, etc.) in `internal/telemetry/telemetry.go`, metrics servers on `:8082` and `:8083` for delivery and dispatcher daemons |
| **CI Wiring & Go Suite** | Github actions integration, build/vet/test verification, go mod tidy | Complete; wired to GitHub actions `.github/workflows/ci.yml` under the `smoke` job, codebase compiles and vets cleanly, and the default Go unit test suite passes successfully |
| **Critical Security & Correctness Fixes (§8 P0/P1)** | SNS signature bypass removal, transient retry, rate limiter singleton, compiler arg alignment | Complete; removed prod SNS signature bypass and injected verifier interface, restricted SNS hosts and verified topic ARN, implemented transient retry with attempt clearing, instantiated SES adapter/limiter once per daemon process, returned correctly-aligned compiler arg lists, and updated store ErrNotFound check |

The milestone is complete only when every row is supported by direct current-state evidence.
