# V1 Milestone 16 completion audit

Authoritative scope: AI Depth — Governed Agentic Assistant, Analytics-Insight Copilot, Expanded Tools & Prompt Management in [`v1-milestone-16-plan.md`](./v1-milestone-16-plan.md).

| Requirement | Evidence required | Current status |
|---|---|---|
| **21.0.1 Connected-content SSRF-safe + allowlisted + fallback** | `internal/render/fetcher.go` validates host, blocks private IPs, degrades to fallback; `go test -race ./internal/render/` clean | Complete; [`fetcher.go`](../../internal/render/fetcher.go) verified; `TestSecurityE2E` in [`catalogs_security_e2e_integration_test.go`](../../internal/httpapi/catalogs_security_e2e_integration_test.go) passes private-IP blocked, unlisted host refused, and fallback tests; `go test -race ./internal/render/` clean. |
| **21.0.2 Secrets ref-only + cache bounded** | `auth_secret_ref` rejects raw secrets and redacts on read; TTL cache size-bounded and race-free | Complete; `TestSecurityE2E/2` in [`catalogs_security_e2e_integration_test.go`](../../internal/httpapi/catalogs_security_e2e_integration_test.go) verifies raw secret rejection and GET redaction; [`cache_test.go`](../../internal/render/cache_test.go) verifies bounded size, TTL expiration, and concurrent access; `go test -race ./internal/render/` clean. |
| **21.0.3 M15 review findings** | Fold any concrete findings from M15 review; every finding has fix + test or verified-safe | Complete; post-M15 review clean (670 Go / 310 web / 30 SDK tests pass, `-race` clean, no new deps); verified safe with no additional findings to fold. |
| **21.1.1 Bounded agent loop foundation** | `internal/ai/agent` bounded ReAct loop wiring `gateway.Generate` + `tools.Runner.Call`, capped by `maxSteps`, timeout, budget; read-only tools only; scope-checked + audited | Complete; [`agent.go`](../../internal/ai/agent/agent.go) implements ReAct loop bounded by maxSteps, budget, wall-clock context timeout, actor scope-intersection via `deriveAgent`, and append-only activity audit; [`agent_test.go`](../../internal/ai/agent/agent_test.go) tests multi-step tool call execution, runaway loop cap termination, scope denial audit, and budget/timeout bounds. |
| **21.2.1 Expanded read-only tools** | `report.timeseries` (M14 reports), `catalog.lookup`, `flag.evaluate`, `journey.inspect` implemented in `internal/ai/tools`, registered in `ReadOnlyTools()`, scope-gated + audited | Complete; [`tools.go`](../../internal/ai/tools/tools.go):310-480 implements `report.timeseries`, `catalog.lookup`, `flag.evaluate`, and `journey.inspect` tools; added to `ReadOnlyTools()`; [`tools_test.go`](../../internal/ai/tools/tools_test.go) (`TestExpandedReadOnlyTools`) verifies store resolution, JSON schema validation, scope enforcement (`reports:read`, `catalogs:read`, `flags:read`, `journeys:read`), activity audit, and zero state mutation. |
| **21.3.1 Analytics-insight copilot** | `POST /v1/ai/copilots/insights` driving agent over report tools; seeded `analytics-insight` prompt; metric citation grounding via `reportContainsValue` | Complete; [`ai_copilot_insights.go`](../../internal/httpapi/ai_copilot_insights.go) implements insights copilot endpoint; [`ai_seed.go`](../../internal/httpapi/ai_seed.go) seeds `analytics-insight` prompt; domain validator reuses `reportContainsValue` to enforce citation grounding; [`ai_copilot_insights_test.go`](../../internal/httpapi/ai_copilot_insights_test.go) (`TestInsightsCopilotHandler`) tests response structure, activity audit, tool trace, and ungrounded metric rejection/repair. |
| **21.4.1 Prompt CRUD + eval + publish routes** | `GET/POST/PUT/DELETE /v1/ai/prompts`, `.../{id}/versions`, eval-run route, `.../versions/{vid}/publish` (human-gated) in `internal/httpapi/prompts.go`; `prompts:read`/`prompts:write` | Complete; [`prompts.go`](../../internal/httpapi/prompts.go) implements prompt and version CRUD, eval status update, and human-gated publish endpoints (`prompts.Publish`); [`server.go`](../../internal/httpapi/server.go) routes guarded with `prompts:read`/`prompts:write`; [`prompts_test.go`](../../internal/httpapi/prompts_test.go) (`TestPromptManagementHTTP`) verifies round-trip, scope 403 enforcement, non-human publish rejection (403), and eval pass requirement. |
| **21.5.1 Prompts UI section** | `web/src/sections/Prompts.tsx` on M12 library; `api.ts` wrappers; 6-point registration across `App.tsx`/`Sidebar.tsx`/`CommandPalette.tsx`; `Prompts.test.tsx` green | Complete; [`Prompts.tsx`](../../web/src/sections/Prompts.tsx) implements prompt listing, version authoring, JSON parameter/schema editing with `JsonField`, eval run, and human-gated publish modal; [`api.ts`](../../web/src/api.ts) wraps prompt management API; 6-point registration complete; [`Prompts.test.tsx`](../../web/src/sections/Prompts.test.tsx) (5 tests) verifies author/eval/publish flow on M12 primitives. |
| **21.6.1 Copilot UI refresh + inline draft review** | Refactor `web/src/sections/Copilots.tsx` to M12 components (Card, Field, Button, Modal, Toast); review generated draft inline (accept/refine in place) | Complete; [`Copilots.tsx`](../../web/src/sections/Copilots.tsx) refactored to M12 primitives with inline draft review cards and accept/refine actions replacing hash navigation; [`Copilots.test.tsx`](../../web/src/sections/Copilots.test.tsx) (6 tests) verifies inline draft acceptance, refinement, and execution. |
| **21.7.1 Assistant UI section** | `web/src/sections/Assistant.tsx` analytics assistant rendering grounded answer + audited tool trace (with `activity_id` links); M12 primitives + Chart; 6-point registration | Complete; [`Assistant.tsx`](../../web/src/sections/Assistant.tsx) implements conversational assistant UI rendering grounded answers, key metrics, and tool-use trace steps linked to `ai_activity`; 6-point registration complete; [`Assistant.test.tsx`](../../web/src/sections/Assistant.test.tsx) (4 tests) covers query submit, grounded answer display, and tool trace rendering. |
| **21.8.1 AI-depth E2E** | E2E integration test for assistant question -> bounded tool loop -> grounded answer, insights copilot over M14 reports, and prompt authoring -> eval -> publish -> use | Complete; [`ai_depth_e2e_integration_test.go`](../../internal/httpapi/ai_depth_e2e_integration_test.go) implements `TestAIDepthE2E` covering assistant tool loop answer generation, citation-grounded insights, and prompt lifecycle integration. |
| **21.8.2 AI-depth Security E2E** | Security E2E test verifying agent bounds (maxSteps/budget/timeout), read-only scope enforcement (`denied_scope` audit), ungrounded insight rejection (422), human-gated prompt publish (non-human 403), PII redaction before egress | Complete; [`ai_depth_security_e2e_integration_test.go`](../../internal/httpapi/ai_depth_security_e2e_integration_test.go) implements `TestAIDepthSecurityE2E` covering runaway loop caps, over-scoped tool denial + audit, ungrounded insight 422 rejection, non-human prompt publish 403, and PII egress redaction. |
| **21.8.3 Run full test suite & dependency verification** | Full test suite green (`go test ./...`, web `npm test`, SDK `npm test`); zero dependency additions in `go.mod`, `go.sum`, `package.json` | Complete; all Go packages green, 313 web tests pass (`npm run typecheck`, `npm run build`, `npm test`), 30 JS SDK tests pass (`npm run build`, `npm test`), `git diff` confirms zero dependency additions across all lockfiles. |
| **21.8.4 Audit doc** | `docs/milestones/v1-milestone-16-audit.md` exists with one row per 21.x task + evidence | Complete; this document. |

**Summary**

Milestone 16 is complete. OpenJourney now features a governed AI Depth layer:
1. **Governed Agentic Assistant (`21.1`)**: A bounded ReAct loop (`internal/ai/agent`) wiring `ai.Gateway.Generate` and `tools.Runner.Call`. The loop is hard-capped by step count, wall-clock timeout, and Gateway monthly/per-call budgets. Principals are downgraded to `ActorType:"ai_agent"` with scope-intersection (`deriveAgent`), enforcing read-only tool execution and append-only activity logging for every step.
2. **Expanded Read-Only Tools (`21.2`)**: Implemented `report.timeseries` (M14 over-time/retention/growth/cost reports), `catalog.lookup`, `flag.evaluate`, and `journey.inspect` in `internal/ai/tools` registered into `ReadOnlyTools()`, all scope-gated and audited with zero state mutation.
3. **Analytics-Insight Copilot (`21.3`)**: `POST /v1/ai/copilots/insights` drives the agent over report tools with anti-hallucination metric citation grounding (`reportContainsValue`), rejecting and repairing ungrounded numeric claims.
4. **Prompt Management (`21.4` & `21.5`)**: Backend HTTP CRUD, eval status management, and human-gated publish routes (`internal/httpapi/prompts.go`) paired with an admin UI section (`Prompts.tsx`) built on M12 primitives for authoring, evaluating, and publishing prompt versions (`prompts:read`/`prompts:write`).
5. **Copilot UI Refresh (`21.6`)**: Migrated `Copilots.tsx` to M12 component primitives with inline draft review (in-place accept/refine flow) replacing hash navigation.
6. **Assistant UI Section (`21.7`)**: Implemented `Assistant.tsx` for conversational analytics inquiries, displaying citation-grounded answers and audited tool-use trace steps linked to `ai_activity` records.
7. **Closeout & E2E Verification (`21.0` & `21.8`)**: Verified M15 connected-content security invariants (SSRF protection, raw secret rejection, bounded cache). Added comprehensive E2E integration and Security E2E test suites (`ai_depth_e2e_integration_test.go`, `ai_depth_security_e2e_integration_test.go`) proving agent bounds, read-only scope isolation, citation enforcement, human-gated publish, and PII redaction.

All 14 sub-tasks (21.0.1–21.8.4) are complete with verifying tests. All Go packages pass, 313 web tests pass, and 30 SDK tests pass. Zero new dependencies added. AI Depth milestone complete.
