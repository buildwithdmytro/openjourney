# V1 milestone 9 completion audit

Authoritative scope: Extension Ecosystem — Signed Remote & WASI Extensions in [`v1-milestone-9-plan.md`](./v1-milestone-9-plan.md).

| Requirement | Evidence required | Current status |
|---|---|---|
| **14.0 Foundations: signed manifest registry, configuration, and grants** | Versioned immutable manifests, publisher-signature verification, human install gate, secret references, scope intersection, migration tables, API-key scopes, and `connector.run` constraint | Complete; [`registry.go`](../../internal/extension/registry.go), [`extensions.go`](../../internal/postgres/extensions.go), and `TestExtensionsIntegration` in [`extensions_integration_test.go`](../../internal/postgres/extensions_integration_test.go) prove signed/invalid manifest handling, immutable publication, and human-only installation. [`resolver.go`](../../internal/extension/resolver.go) and `TestExtensionConfigAndGrants_14_0_3` prove grants intersect requested scopes and resolve `_ref`/`_FILE` secrets. [`041_extensions.sql`](../../internal/postgres/migrations/041_extensions.sql), `rbac.go`, and `TestExtensionsMigrationAndDefaultScopesIntegration_14_0_1` cover schema, scopes, and `connector.run`. |
| **14.1 Bounded remote-invocation host** | Derived extension principal, rate/budget limits, signed HTTP, SSRF/allowlist guard, timeout, circuit breaker, and exactly-one activity record | Complete; [`host.go`](../../internal/extension/host.go), [`042_extension_activity.sql`](../../internal/postgres/migrations/042_extension_activity.sql), and the `TestHostInvoke_*` suite in [`host_test.go`](../../internal/extension/host_test.go) prove success, off-allowlist blocking, timeout fallback signal, breaker opening, rate/budget denial, and disabled short-circuit with audit records. |
| **14.2 Remote channel-provider extension** | Existing channel registry/adapter seam delegates sends through the bounded host and maps failures to `DeliveryError` | Complete; [`channel.go`](../../internal/extension/channel.go), `TestExtensionChannelAdapter_Send_Success`, `TestExtensionChannelAdapter_Send_FailureWrapsInDeliveryError`, and `TestRegisterChannelProviders` in [`channel_test.go`](../../internal/extension/channel_test.go) prove registration, bounded host delegation, auditing, and normal delivery errors. |
| **14.3 Remote journey action/condition nodes** | Pinned extension action/condition nodes validate declarations and take deterministic fallback branches on failure without dead-lettering | Complete; [`nodes.go`](../../internal/journey/nodes.go), [`nodes_test.go`](../../internal/journey/nodes_test.go), and `TestExtensionNodesExecution_Success`, `TestExtensionNodesExecution_FallbackOnError`, `TestExtensionNodesExecution_TimeoutAndCircuitOpenUseFallback`, and `TestExtensionNodesExecution_Validation` prove valid branches, fallback advancement, bounded failures, and publish validation. |
| **14.4 WASI/Wasm sandbox** | Deterministic JSON execution, memory/deadline bounds, and no host network/filesystem access | Complete; [`wasm.go`](../../internal/extension/wasm.go), [`wasm_test.go`](../../internal/extension/wasm_test.go), and `TestWasm_Deterministic`, `TestWasm_DeadlineKill`, `TestWasm_NoFileAccess` plus `TestSecurityE2E_WasmCannotReachNetworkOrFilesystem` in [`security_test.go`](../../internal/extension/security_test.go) prove repeatable output, deadline termination, and sandbox isolation. `go.mod` contains the sole new runtime dependency, `wazero`. |
| **14.5 Ingestion-transform extension** | Pre-accept subscribed transform hook enriches/annotates only; deterministic reject/passthrough behavior preserves identity and consent | Complete; [`ingestion.go`](../../internal/httpapi/ingestion.go), [`ingestion_test.go`](../../internal/httpapi/ingestion_test.go), and `TestIngestionTransformEnrichesAndPreservesEventIdentity`, `TestIngestionTransformFailureUsesConfiguredPolicy`, and `TestIngestionTransformDeadlineTrapNeverStallsAndUsesPassthrough` prove the hook placement, enrichment boundary, configured failure policy, and deadline-safe passthrough. |
| **14.6 Template-function extension** | Liquid filters/tags are discovered from signed manifests and execute through the bounded deterministic Wasm host with audit | Complete; [`template.go`](../../internal/extension/template.go), [`render.go`](../../internal/render/render.go), `TestTemplateFunctionFilterUsesWasmAndAudits`, and `TestTemplateFunctionDiscoveryRegistersTag` in [`template_test.go`](../../internal/extension/template_test.go) prove filter rendering, tag discovery, Wasm execution, and activity recording. |
| **14.7 Outbound connector extension** | `events.accepted.v1` outbox subscriptions create deduplicated leased `connector.run` jobs, which invoke the remote connector with stable event idempotency | Complete; [`store.go`](../../internal/postgres/store.go), [`operations.go`](../../internal/operations/operations.go), [`connector_test.go`](../../internal/operations/connector_test.go), and `TestConnectorJobIsLeasedAndCompletedAfterBoundedInvocation` / `TestConnectorRunUsesStablePerEventIdempotencyInput` prove enqueue, lease/complete, bounded invocation, and per-event idempotency. |
| **14.8 Governance, health, and kill switch** | Tenant-scoped activity endpoint, authorization, health state, disabled kill switch, breaker, rate, and budget governance | Complete; [`extensions.go`](../../internal/httpapi/extensions.go), [`server.go`](../../internal/httpapi/server.go), [`host.go`](../../internal/extension/host.go), and the activity authorization tests in [`server_test.go`](../../internal/httpapi/server_test.go) plus `TestHostInvoke_DisabledExtensionStopsBeforeTransport` in [`host_test.go`](../../internal/extension/host_test.go) prove scope enforcement, tenant/workspace activity isolation, and kill-switch behavior. |
| **14.9 Extension registry UI** | Theme-aware install/review/grant/configure/enable/disable/activity surfaces use existing frontend patterns without a new dependency | Complete; [`Extensions.tsx`](../../web/src/sections/Extensions.tsx), [`api.ts`](../../web/src/api.ts), and the recorded `npm run typecheck`, `npm run build`, and 32-test frontend suite in task 14.9 evidence cover the registry/editor workflows. |
| **14.10 Integration, security, determinism, and audit closeout** | Unsigned/tampered rejection, denied-scope audit, SSRF blocking, Wasm isolation/deadline, deterministic fallback/output, append-only allowed/denied activity, and green verification suite | Complete; `TestSecurityE2E_RequiredScopeIsDeniedAndAudited` and `TestSecurityE2E_WasmCannotReachNetworkOrFilesystem` in [`security_test.go`](../../internal/extension/security_test.go), the bounded-failure tests in [`nodes_test.go`](../../internal/journey/nodes_test.go) and [`ingestion_test.go`](../../internal/httpapi/ingestion_test.go), `TestWasm_Deterministic` in [`wasm_test.go`](../../internal/extension/wasm_test.go), and `TestExtensionActivityHardening_14_10_3` in [`extensions_integration_test.go`](../../internal/postgres/extensions_integration_test.go) provide direct security, fallback, determinism, and append-only evidence. The plan records the completed Go build/vet/test, `go mod tidy`, web typecheck/build/tests, and zero-vulnerability `npm audit`. |

Milestone 9 is complete. Extensions are signed and versioned, scope-governed, bounded, SSRF-guarded, Wasm-sandboxed, auditable, and connected through the existing channel, journey, ingestion, rendering, and outbox seams.
